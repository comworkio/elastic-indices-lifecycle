stages:
  - publish

github-mirror:
  stage: publish
  script:
    - setsid ./ci/github-mirror.sh
  only:
    - /^(master.*)$/
  tags:
    - elasticlifecycle

docker-deliver:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh
  only:
    - /^(master.*)$/
  tags:
    - elasticlifecycle

