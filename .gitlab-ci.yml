stages:
  - publish

mirror:
  stage: publish
  script:
    - setsid ./ci/mirror.sh
  only:
    - /^(master.*)$/
  tags:
    - elasticlifecycle

rollup-x86:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "x86" "rollup" "7.15.1-1.0"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - .gitlab-ci.yml
      - rollup/*
      - requirements.txt
      - Dockerfile
      - ci/docker-deliver.sh
  tags:
    - elasticlifecycle

rollup-arm:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "arm" "rollup" "7.15.1-1.0"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - .gitlab-ci.yml
      - rollup/*
      - requirements.txt
      - Dockerfile.arm
      - ci/docker-deliver.sh
  tags:
    - buildarm

backup-x86:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "x86" "backup" "7.15.1-1.0"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - .gitlab-ci.yml
      - backup/*
      - requirements.txt
      - Dockerfile
      - ci/docker-deliver.sh
  tags:
    - elasticlifecycle

backup-arm:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "arm" "backup" "7.15.1-1.0"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - .gitlab-ci.yml
      - backup/*
      - requirements.txt
      - Dockerfile.arm
      - ci/docker-deliver.sh
  tags:
    - buildarm
